import { NextRequest,NextResponse } from 'next/server'
import { requireAdmin } from '@/lib/auth'
import { createClient } from '@/lib/supabase/server'
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){const membership=await requireAdmin();const {id}=await params;const body=await request.json() as {status?:string;notes?:string};const allowed=new Set(['open','investigating','resolved','dismissed']);if(!body.status||!allowed.has(body.status))return NextResponse.json({error:'Invalid status.'},{status:400});const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();const {error}=await supabase.from('security_threat_events').update({status:body.status,resolution_notes:body.notes?.trim()||null,resolved_at:['resolved','dismissed'].includes(body.status)?new Date().toISOString():null,resolved_by:['resolved','dismissed'].includes(body.status)?user?.id??null:null}).eq('id',id).eq('organization_id',membership.organization_id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}
