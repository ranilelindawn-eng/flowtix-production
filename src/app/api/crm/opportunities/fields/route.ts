import { NextResponse } from 'next/server'
import { requirePermission } from '@/lib/auth'
import { listOpportunityFieldDefinitions,upsertOpportunityFieldDefinition,type OpportunityFieldType } from '@/lib/opportunity-advanced'
import { writeAuditEvent } from '@/lib/security/audit'
const TYPES=new Set<OpportunityFieldType>(['text','number','date','boolean','select','multi_select'])
export async function GET(){ await requirePermission('opportunities.view'); return NextResponse.json({fields:await listOpportunityFieldDefinitions()}) }
export async function POST(request:Request){ const membership=await requirePermission('opportunities.update'); const body=await request.json() as Record<string,unknown>; const fieldType=typeof body.fieldType==='string'&&TYPES.has(body.fieldType as OpportunityFieldType)?body.fieldType as OpportunityFieldType:'text'; const field=await upsertOpportunityFieldDefinition({id:typeof body.id==='string'?body.id:undefined,fieldKey:typeof body.fieldKey==='string'?body.fieldKey:'',label:typeof body.label==='string'?body.label:'',fieldType,options:Array.isArray(body.options)?body.options:[],isRequired:body.isRequired===true,isActive:body.isActive!==false,position:typeof body.position==='number'?body.position:0}); await writeAuditEvent({action:'opportunity_field.upsert',resourceType:'opportunity_field_definition',resourceId:field.id,organizationId:membership.organization_id,metadata:{fieldKey:field.field_key,fieldType:field.field_type}}); return NextResponse.json({field}) }
