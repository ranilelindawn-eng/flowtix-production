import{requireOrganization}from'@/lib/auth';import{createClient}from'@/lib/supabase/server';import{createSnippet}from'../crm-actions'
export default async function SnippetsPage(){const m=await requireOrganization();const s=await createClient();const{data}=await s.from('snippets').select('*').eq('organization_id',m.organization_id).order('created_at',{ascending:false});const f='min-h-11 rounded-xl border border-white/10 bg-[#07111F] px-3 text-sm text-white';return <div className="space-y-6"><header><p className="text-sm uppercase tracking-[.24em] text-cyan-400">Productivity</p><h1 className="mt-2 text-3xl font-semibold text-white">Snippets</h1></header><form action={createSnippet} className="grid gap-3 rounded-2xl border border-white/10 bg-[#0B1726]/90 p-5 md:grid-cols-2"><input required name="name" placeholder="Snippet name" className={f}/><input required name="shortcut" placeholder="Shortcut, e.g. /intro" className={f}/><textarea required name="content" rows={4} placeholder="Reusable text" className={`${f} py-3 md:col-span-2`}/><button className="rounded-xl bg-blue-600 px-4 py-3 text-sm font-semibold text-white md:col-span-2">Create snippet</button></form><div className="grid gap-4 md:grid-cols-2">{data?.map(x=><article key={x.id} className="rounded-2xl border border-white/10 bg-[#0B1726]/90 p-5"><div className="flex items-center justify-between"><h2 className="font-semibold text-white">{x.name}</h2><code className="rounded bg-white/5 px-2 py-1 text-xs text-cyan-300">{x.shortcut}</code></div><p className="mt-3 whitespace-pre-wrap text-sm text-slate-400">{x.content}</p></article>)}</div></div>}
