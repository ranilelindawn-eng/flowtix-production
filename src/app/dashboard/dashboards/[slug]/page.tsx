import Link from 'next/link'
import { ArrowLeft, BarChart3 } from 'lucide-react'
import { getDashboard } from '@/lib/dashboards'
type Props={params:Promise<{slug:string}>}
const format=(value:number|string,kind?:string)=>{if(typeof value==='string')return value;if(kind==='percent')return `${value.toFixed(1)}%`;if(kind==='currency')return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(value);if(kind==='duration')return `${Math.round(value)} sec`;return new Intl.NumberFormat('en-US',{notation:value>999?'compact':'standard',maximumFractionDigits:1}).format(value)}
export default async function DashboardViewPage({params}:Props){const {slug}=await params;const {dashboard,metrics}=await getDashboard(slug);return <div className="space-y-8"><header><Link href="/dashboard/dashboards" className="inline-flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft className="h-4 w-4"/>All dashboards</Link><p className="mt-6 text-sm uppercase tracking-[.24em] text-cyan-400">{dashboard.kind} dashboard</p><h1 className="mt-2 text-3xl font-semibold text-white">{dashboard.name}</h1><p className="mt-2 max-w-3xl text-sm text-slate-400">{dashboard.description}</p></header><section className="grid grid-cols-12 gap-4">{dashboard.layout.map(widget=><article key={widget.id} className="col-span-12 rounded-2xl border border-white/10 bg-[#0B1726]/90 p-6 sm:col-span-6 xl:col-span-3" style={{minHeight:`${Math.max(widget.position.h,1)*90}px`}}><div className="flex items-center justify-between"><p className="text-sm text-slate-400">{widget.title}</p><BarChart3 className="h-4 w-4 text-cyan-400"/></div><p className="mt-4 text-3xl font-semibold text-white">{format(metrics[widget.metric]??0,widget.format)}</p>{widget.description&&<p className="mt-2 text-xs text-slate-500">{widget.description}</p>}{widget.href&&<Link href={widget.href} className="mt-5 inline-block text-sm font-medium text-cyan-300 hover:text-cyan-200">Open details →</Link>}</article>)}</section></div>}
export const dynamic='force-dynamic'
