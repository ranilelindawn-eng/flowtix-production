import { Smartphone } from 'lucide-react'
import { canManageSettings, requireSettingsContext } from '@/lib/settings-context'
import { addPhoneNumber, removePhoneNumber, setDefaultPhoneNumber } from './actions'
export default async function PhoneNumbersPage(){const {supabase,organizationId,role}=await requireSettingsContext();const manageable=canManageSettings(role);const {data:numbers}=await supabase.from('organization_phone_numbers').select('*').eq('organization_id',organizationId).order('created_at',{ascending:false});return <div className="space-y-8"><div><h1 className="text-3xl font-bold">Phone Numbers</h1><p className="mt-2 text-muted-foreground">Assign provider numbers, capabilities, and the default outbound caller ID.</p></div>{manageable?<form action={addPhoneNumber} className="rounded-xl border border-border bg-card p-6"><div className="flex items-center gap-3"><Smartphone className="h-5 w-5 text-primary"/><h2 className="text-xl font-semibold">Add phone number</h2></div><div className="mt-5 grid gap-4 md:grid-cols-3"><input name="friendly_name" placeholder="Sales line" className="rounded-lg border bg-background px-3 py-2"/><input name="phone_number" required placeholder="+15551234567" className="rounded-lg border bg-background px-3 py-2"/><select name="provider" className="rounded-lg border bg-background px-3 py-2"><option>twilio</option><option>telnyx</option><option>signalwire</option><option>plivo</option></select></div><div className="mt-4 flex gap-5 text-sm"><label><input type="checkbox" name="voice" defaultChecked/> Voice</label><label><input type="checkbox" name="sms" defaultChecked/> SMS</label></div><button className="mt-5 rounded-lg bg-primary px-4 py-2 font-medium text-primary-foreground">Add Number</button></form>:null}<div className="grid gap-4">{(numbers??[]).map(n=><article key={n.id} className="rounded-xl border border-border bg-card p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><h2 className="font-semibold">{n.friendly_name}</h2>{n.is_default?<span className="rounded-full bg-primary/10 px-2 py-1 text-xs text-primary">Default</span>:null}</div><p className="mt-1 font-mono text-sm">{n.phone_number}</p><p className="mt-1 text-sm text-muted-foreground capitalize">{n.provider} · {Object.entries((n.capabilities??{}) as Record<string,boolean>).filter(([,v])=>v).map(([k])=>k).join(', ')}</p></div>{manageable?<div className="flex gap-3">{!n.is_default?<form action={setDefaultPhoneNumber}><input type="hidden" name="id" value={n.id}/><button className="rounded-lg border px-3 py-2 text-sm">Set default</button></form>:null}<form action={removePhoneNumber}><input type="hidden" name="id" value={n.id}/><button className="rounded-lg border border-destructive/40 px-3 py-2 text-sm text-destructive">Remove</button></form></div>:null}</div></article>)}{!numbers?.length?<p className="rounded-xl border border-dashed p-8 text-center text-muted-foreground">No phone numbers configured.</p>:null}</div><p className="text-sm text-muted-foreground">Purchasing or releasing a carrier-owned number must be completed through your Twilio, Telnyx, SignalWire, or Plivo account unless provider API credentials are configured.</p></div>}
